name: Reusable CI/CD

on:
  workflow_call:
    inputs:
      python-version:
        required: true
        type: string
      os:
        required: true
        type: string
    secrets:
      codecov_token:
        required: true

defaults:
  run:
    shell: bash

jobs:
  formatting:
    runs-on: ${{ inputs.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install dependencies
        run: uv sync --group dev
      - name: Formatting
        run: |
          uv run black --diff --check .
          uv run isort --diff --check .

  linting:
    runs-on: ${{ inputs.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install dependencies
        run: uv sync --group dev --group test
      - name: PyLint
        run: uv run pylint poutyne/ tests/
      - name: Flake8
        run: uv run flake8 poutyne/ tests/

  docs:
    runs-on: ${{ inputs.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install dependencies
        run: uv sync --group docs
      - name: Building doc
        run: uv run make -C docs clean html

  tests:
    runs-on: ${{ inputs.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install dependencies
        run: uv sync --group test
      - name: Test with pytest
        run: uv run pytest
      - name: Upload Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          token: ${{ secrets.codecov_token }}

  tests_without_deps:
    runs-on: ${{ inputs.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up uv
        uses: astral-sh/setup-uv@v4
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install dependencies
        run: |
          uv venv
          uv pip install pytest pytest-cov -e .
      - name: Test with pytest
        run: uv run pytest
